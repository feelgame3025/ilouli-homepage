---
argument-hint: [ComponentName]
description: 새 React 컴포넌트 생성
---

"$ARGUMENTS" 이름으로 새 React 컴포넌트를 생성해줘:

1. `frontend/src/components/$ARGUMENTS.js` 파일 생성
2. `frontend/src/components/$ARGUMENTS.css` 파일 생성
3. 기본 함수형 컴포넌트 구조 작성
4. CSS import 포함
5. export default 포함

컴포넌트 이름이 없으면 어떤 컴포넌트를 만들지 물어봐줘.
